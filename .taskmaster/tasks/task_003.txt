# Task ID: 3
# Title: Implement Data Models and Types
# Status: done
# Dependencies: 1
# Priority: high
# Description: Define TypeScript interfaces and types for all core entities based on domain model
# Details:
Create src/types/index.ts with interfaces for Event, User, Topic, Vote, Round, Room, ActivityState, Team, AuditLog. Include proper typing for role-based access (guest, participant, organizer, admin), event status enum, activity types enum (voting, intelligence, discussion, teams), and vote weights (first, second, third). Add validation schemas using zod for runtime type checking.

# Test Strategy:
Verify all types compile correctly, zod schemas validate expected data structures, and type safety is maintained across the application

# Subtasks:
## 1. Define Core Entity Interfaces [done]
### Dependencies: None
### Description: Create TypeScript interfaces for Event, User, Topic, and Vote entities
### Details:
Define base interfaces for Event (id, title, description, status, createdAt, etc.), User (id, name, email, role, etc.), Topic (id, title, description, submittedBy, votes, etc.), and Vote (id, userId, topicId, weight, timestamp, etc.). Include proper typing for relationships between entities.

## 2. Create Activity State and Role Enums [done]
### Dependencies: None
### Description: Define enums for event status, activity types, user roles, and vote weights
### Details:
Create EventStatus enum (draft, active, paused, completed), ActivityType enum (voting, intelligence, discussion, teams), UserRole enum (guest, participant, organizer, admin), and VoteWeight enum (first, second, third). Export all enums with proper TypeScript typing.

## 3. Define Round, Room, and Team Interfaces [done]
### Dependencies: 3.1, 3.2
### Description: Create interfaces for session management and team organization
### Details:
Define Round interface (id, eventId, activityType, startTime, endTime, settings), Room interface (id, eventId, participants, currentActivity, state), and Team interface (id, name, members, roomId, score). Include proper references to core entities and activity states.

## 4. Add AuditLog and Analytics Types [done]
### Dependencies: 3.1, 3.2
### Description: Create interfaces for audit logging and analytics tracking
### Details:
Define AuditLog interface (id, userId, action, entityType, entityId, timestamp, metadata), Analytics interface for event metrics, and ActivityState interface for real-time state management. Include proper typing for action types and metadata structures.

## 5. Install and Configure Zod for Validation [done]
### Dependencies: 3.1, 3.2, 3.3, 3.4
### Description: Set up zod library and create validation schemas for all interfaces
### Details:
Install zod package, create validation schemas for all defined interfaces (EventSchema, UserSchema, TopicSchema, VoteSchema, etc.). Add runtime validation functions and error handling for schema validation failures. Include proper type inference from schemas.

## 6. Create Type Exports and Validation Functions [done]
### Dependencies: 3.5
### Description: Organize exports and create utility functions for type validation
### Details:
Create centralized exports in src/types/index.ts for all interfaces, enums, and schemas. Add utility functions for type validation, type guards, and data transformation. Include helper functions for role-based access checking and entity relationship validation.

